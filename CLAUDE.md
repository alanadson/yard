# CLAUDE.md

The instructions for this repository live in [`AGENTS.md`](./AGENTS.md) — a
single file, for every agent and every human. Read it before your first `Edit`.

The summary, in case you only read this: **no production code without a test
that failed first**, in TypeScript and in Rust. The cycle, the recipes per
layer and the definition-of-done checklist are there; the detail with examples
from the code itself is in [`docs/specs/06-tdd.md`](./docs/specs/06-tdd.md).

@AGENTS.md
